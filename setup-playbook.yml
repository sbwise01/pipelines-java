---

- hosts: localhost
  gather_facts: no
  become: true
  become_user: root
  become_method: sudo


  tasks:
  - name: Install curl
    yum:
      name: curl
      state: latest
      use_backend: yum3
