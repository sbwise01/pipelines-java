---

- hosts: localhost
  gather_facts: no
  vars:
    otherfile: "{{ lookup('env','otherfile') }}"

  tasks:
  - name: touch other file
    file:
      path: "~/{{ otherfile }}"
      state: touch
      mode: 0600
